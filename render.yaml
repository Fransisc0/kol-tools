services:
  - type: web
    name: fransisc0-kol-tools-tcrs-api
    runtime: node
    plan: free
    region: virginia
    branch: main
    autoDeploy: true
    buildCommand: npm ci --include=dev && npm run typecheck && npm test && npm run clean && npm run build:server && npm prune --omit=dev
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 22.13.0
      - key: NODE_ENV
        value: production
      - key: ALLOWED_ORIGIN
        value: https://fransisc0.github.io
      - key: SERVE_CLIENT
        value: 'false'
