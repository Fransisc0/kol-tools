{
  "name": "kol-tools",
  "private": true,
  "version": "1.0.1",
  "description": "Privacy-conscious, open-source tools for Kingdom of Loathing players.",
  "license": "BSD-3-Clause",
  "homepage": "https://fransisc0.github.io/kol-tools/",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Fransisc0/kol-tools.git"
  },
  "bugs": {
    "url": "https://github.com/Fransisc0/kol-tools/issues"
  },
  "type": "module",
  "packageManager": "npm@10.9.2",
  "engines": {
    "node": ">=22.13.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "predev": "node --experimental-sqlite --import tsx scripts/ensure-dev-data.ts",
    "dev": "vite",
    "clean": "rimraf dist coverage .vite public/data",
    "generate:tcrs": "node --experimental-sqlite --import tsx scripts/build-generated-static-data.ts",
    "data:download": "tsx scripts/download-dol.ts",
    "data:check": "tsx scripts/check-current-inputs.ts",
    "prepare:data": "node --experimental-sqlite --import tsx scripts/build-generated-static-data.ts",
    "typecheck": "tsc -p tsconfig.client.json --noEmit && tsc -p tsconfig.tools.json --noEmit",
    "lint": "eslint . --max-warnings=0",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "build:client": "vite build",
    "build:hub": "node scripts/build-pages.mjs",
    "build": "npm run clean && npm run prepare:data && npm run build:client && npm run build:hub",
    "build:staged": "npm run clean && npm run prepare:data -- --kolmafia-root .upstream/kolmafia --dol-database .staged-data/dol.sqlite --dol-etag-file .staged-data/dol.etag && npm run verify:data && npm run build:client && npm run build:hub",
    "preview": "vite preview --outDir dist/pages/tcrs",
    "verify:pages": "node scripts/verify-pages.mjs",
    "verify:data": "tsx scripts/verify-generated-datasets.ts",
    "verify:data-determinism": "node --experimental-sqlite --import tsx scripts/verify-generated-determinism.ts",
    "verify:privacy": "node scripts/privacy-scan.mjs",
    "verify:code": "npm run typecheck && npm run lint && npm run format:check && npm test && npm audit --audit-level=high && npm audit --omit=dev --audit-level=high",
    "verify": "npm run verify:code && npm run build && npm run verify:data && npm run verify:data-determinism && npm run verify:pages && npm run verify:privacy"
  },
  "dependencies": {
    "lucide-react": "^0.546.0",
    "react": "^19.0.1",
    "react-dom": "^19.0.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@tailwindcss/vite": "^4.1.14",
    "@types/node": "^22.14.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^6.1.1",
    "autoprefixer": "^10.4.21",
    "eslint": "^10.10.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.7",
    "globals": "^16.0.0",
    "prettier": "^3.9.8",
    "rimraf": "^6.1.3",
    "tailwindcss": "^4.1.14",
    "tsx": "^4.21.0",
    "typescript": "~5.8.2",
    "typescript-eslint": "^8.70.0",
    "vite": "^6.2.3",
    "vitest": "^5.0.1"
  }
}
